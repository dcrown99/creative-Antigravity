---
description: MT5 EA のバックテスト自動実行と結果判定
---

// turbo-all

1. `.agent/skills/mt5-backtest/SKILL.md` を読む
2. 指定に応じてスクリプトを実行:
   - パリティ検証: `.\.agent\skills\mt5-backtest\scripts\run_backtest.ps1 -EA <EA名> -Mode parity -PythonPF <値>`
   - バリデーション: `.\.agent\skills\mt5-backtest\scripts\run_backtest.ps1 -EA <EA名> -Mode validate`
