# TICKET_20260212_1755 - Cold Start Performance Improvement

## 変更範囲

### 案A: Named Volume 化
| ファイル | 変更内容 |
|---|---|
| `docker-compose.dev.yml` | anonymous volume → named volume `mm_next_cache` + トップレベル volumes 追加 |

### 案B: recordDailyHistory cron 分離
| ファイル | 変更内容 |
|---|---|
| `src/lib/cron/history-scheduler.ts` | **[NEW]** 日次履歴記録 cron スケジューラー (23:50 JST) |
| `src/app/api/cron/record-history/route.ts` | **[NEW]** 履歴記録 API エンドポイント |
| `src/instrumentation.ts` | `startHistoryScheduler()` を追加 |
| `src/app/page.tsx` | `recordDailyHistory()` を Promise.all から削除 (5→4 並列) |
| `src/app/analytics/page.tsx` | `recordDailyHistory()` を Promise.all から削除 (4→3 並列) |

### 意図的に変更しなかったファイル
| ファイル | 理由 |
|---|---|
| `src/services/analytics.service.ts` L126-127 | `getAssetHistory()` 内の `recordDailyHistory()` フォールバック呼び出しは安全弁として残す |

## テスト通過

```
PASS src/services/__tests__/analytics.service.test.ts
  AnalyticsService
    getLatestAnalysis
      √ should return null if no log found (2 ms)
      √ should return parsed log (1 ms)
      √ should handle JSON parse error (27 ms)
    recordDailyHistory
      √ should skip if history exists and not forced (2 ms)
      √ should record history if forced (2 ms)
    getAssetAllocation
      √ should calculate allocation correctly (1 ms)

Test Suites: 1 passed, 1 total
Tests:       6 passed, 6 total
```

## ブラウザ検証

- ✅ ダッシュボード(`localhost:3001`): 正常表示。総資産額・資産推移チャート表示確認
- ✅ 分析レポート(`localhost:3001/analytics`): 正常表示。グラフ・資産配分表示確認
- ⚠️ Hydration Mismatch (1 error): UIライブラリ由来。今回の変更とは無関係

## 備考

- 案A の効果はコンテナ再作成後の初回アクセス時に体感できる
- 案B の cron は 23:50 JST に自動実行。手動テストは `POST http://localhost:3001/api/cron/record-history` で可能
