# 検証チケット: Fix Next.js Revalidation Error in Cron

**Ticket ID:** TICKET_20251219_0840
**Date:** 2025-12-19 08:40
**Author:** Antigravity (Gemini 3 Pro)

## 🔄 変更範囲 (Scope of Changes)
- `apps/money-master/src/services/asset.service.ts`:
  - `revalidateTag` をラップする `safeRevalidateTag` ヘルパー関数を追加。
  - バックグラウンドジョブ（Cron）実行時に発生する "Invariant: static generation store missing" エラーを検知し、安全にスキップ（警告ログ出力のみ）するように修正。

## ✅ 検証チェックリスト (Verification Checklist)

### 1. 静的解析 (Static Analysis)
- [x] **Lint Check:** `pnpm turbo run lint --filter=money-master` -> Success (Exit code 0)

### 2. 動作論理 (Logic Verification)
- [x] **Context Analysis:**
  - エラー原因: `node-cron` によるスケジュール実行は Next.js の Request Context 外で動作するため、`revalidateTag` が失敗する。
  - 修正論理: `try-catch` ブロックで当該エラーを捕捉し、プロセスをクラッシュさせずに処理を継続させる。
  - 副作用: バックグラウンド更新時はキャッシュが即時無効化されないが、DB更新は成功するため、次回のデータフェッチ（または時間経過）で反映される。これは許容範囲内。

## 🛡️ 品質保証 (Quality Assurance)
- [x] **Regression Risk:** 低。通常のAPI経由の更新（UI操作）ではコンテキストが存在するため、`revalidateTag` は正常に機能し続ける（catchブロックに入らない）。

## 📝 備考 (Notes)
- ユーザー報告の `Last Updated: 2025/12/18 19:00:00` はスケジュール設定時刻と一致しており、原因特定を裏付けている。
