# TICKET_20251230_0940

## 変更範囲
- `apps/money-master/src/lib/validation.ts` - Zodスキーマ新規作成
- `apps/money-master/src/lib/actions.ts` - バリデーション適用
- `apps/money-master/src/app/api/admin/normalize-dates/route.ts` - 環境変数チェック + Dividend対応

## 実施内容
### 1. Zodバリデーション
- `TransactionSchema` / `DividendSchema` 作成
- 日付フォーマット `YYYY-MM-DD` を強制
- `addTransactionAction` / `addDividend` でsafeParseによるバリデーション

### 2. Debug API改善
- `NODE_ENV === 'production'` で403エラーを返すよう変更
- Dividendテーブルも正規化対象に追加

## チェックリスト
- [x] Lint通過
- [x] Type-check通過
- [x] API検証 (344 transactions, 174 dividends チェック完了)
- [x] フォーム検証 (Add Transactionダイアログ確認)

## 備考
- フロントエンドは `<input type="date">` により日付形式が保証されるため、zodResolver追加は見送り
- サーバーサイドバリデーションで十分にカバー
