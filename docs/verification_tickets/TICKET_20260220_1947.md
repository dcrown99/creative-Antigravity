# Verification Ticket: GitHub Actions Error Fix

## 変更範囲 (Scope of Changes)
- `apps/money-master/src/components/dashboard/LiquidationHeatmap.tsx`: `any` 型の排除。
- `apps/money-master/src/components/dashboard/LagScanner.tsx`: `any` 型の排除。
- `apps/money-master/src/components/crypto/ConfluenceGauge.tsx`: `any` 型の排除。
- `apps/money-master/src/components/analytics/SectorPerformanceChart.tsx`: 未使用の `useState` を削除。
- `apps/money-master/src/components/analytics/RebalanceWidget.tsx`: 未使用の `isLoading` state の未使用警告を無視(Destructuring削除)により修正。
- `apps/money-master/scripts/refine_dividends_to_jpy.ts`: 未使用の `newCurrency` を削除。
- `apps/money-master/scripts/recover_dividends.ts`: `any` 型の排除 (`Unknown`のキャスト修正を含む)。
- `apps/money-master/src/services/__tests__/asset.service.test.ts`: `deleteAsset` テスト内の `mockPrisma.dividend.deleteMany`, `mockPrisma.transaction.updateMany` 呼び出しのためのモック期待値設定。
- `apps/money-master/jest.setup.js`: Prismaモックに `updateMany`, `deleteMany` プロパティを追加。

## ビルド成功確認 (Build Verification)
- `pnpm run lint` が警告のみでエラーなし。
- `pnpm tsc --noEmit` がPASS。

## ブラウザ検証 (Browser Verification)
- フロントエンドコンポーネントのロジックや動作変更はなく、TypeScriptの型定義と未使用変数の削除のみのため対象外。

## テスト通過 (Test Verification)
- `pnpm run test` を実行。
- 166テストすべてPASS (159 passed, 7 skipped). Exit code 0。
